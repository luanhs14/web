# FutTV Frontend - variáveis Vite
# Renomeie para `.env` e ajuste conforme o ambiente desejado.

# URL base da API do backend
VITE_API_URL=http://localhost:3333/api

# URL pública do site (usada em SEO/metadados se necessário)
VITE_PUBLIC_SITE_URL=https://futtv.hserver.pro
