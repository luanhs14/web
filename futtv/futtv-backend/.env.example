# FutTV Backend - variáveis de ambiente
# Renomeie este arquivo para `.env` e ajuste os valores conforme o seu ambiente.

# Configuração básica do servidor HTTP
PORT=3333
NODE_ENV=development
AUTO_SYNC_ON_BOOT=true

# Domínios autorizados para acessar a API (separados por vírgula)
ALLOWED_ORIGINS=http://localhost:5173,https://futtv.hserver.pro

# Banco de dados PostgreSQL (use credenciais gratuitas/local ou Docker)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=futtv
DB_USER=postgres
DB_PASSWORD=postgres

# API gratuita de futebol (https://www.football-data.org/)
# Solicite uma chave gratuita e informe aqui. Sem chave, o backend usa dados de exemplo.
FOOTBALL_DATA_API_TOKEN=
FOOTBALL_DATA_API_URL=https://api.football-data.org/v4
FOOTBALL_DATA_SEASON=2025
